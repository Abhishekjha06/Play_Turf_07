{
  "version": 2,
  "framework": "vite",
  "buildCommand": "cd components && npm run build",
  "outputDirectory": "components/dist",
  "installCommand": "cd components && npm install --production=false && chmod +x node_modules/.bin/vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
